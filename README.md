# Cisco ISR Prompt File Manager

This started out, like so many other projects, as one small thing and as they tend to do, spiraled out of control with what we affectionately call 'scope creep'. While building a war dialing demo for VCFMW using a Cisco 3825/3845 ISR I wanted some of the numbers dialed to feel more authentic to the time period, the 80's and 90's, so I went down the proverbial rabbit hole.

I was pointed to an archive of all the Pat Fleet audio files and they were, naturally, not in a format I could use on the ISR, they needed to be converted to 8-bit au format, so I wrote a script to do that. After a bunch of back and forth with Claude I got a working VoiceXML app that worked, I could dial a number and get a "the number you have dialed is no longer in service" message! But something didn't sound right, there was something missing, those 3 tones at the beginning I remembered being there back in "the day". So, rabbit hole number 4 starts, SIT, I was unable to find any way to generate those on the ISR, it's not a PBX really, so yet another scipt popped up to generate them. Ok, now I have that, how do I combine them? Guess, another script but it worked! Hey, this sounds like it's supposed to! At that point I probably could have called it done but, well, I have a hard time doing that.

I had this pile of shell and Python now that did various bits of the work so I thought, hey, let's use Claude Code to wrangle it all together, so I did. I have to say, it did a really good job at organizing and modifying that pile into something more cohesive. At that point I figured that maybe someone else could use this so why not wrap it into a webapp so it's here as well.

With that, in here are all tools for generating, converting, and serving audio prompt files on a Cisco ISR router. The router plays audio files stored in flash via a VXML application triggered by a VoIP dial-peer. This repo provides everything needed to build those files — from raw ulaw source material through to a web UI that lets you concatenate a SIT tone with a message file and download the result ready for TFTP transfer.

The Pat Fleet message audio files used by this project originate from [hharte/PatFleet-asterisk](https://github.com/hharte/PatFleet-asterisk).

You can self-host the webapp, it's just Flask, or use mine in the INIDEV Pub Crawl [here](https://github.com/youngd24/cisco-isr-pfm). 

---

## Directory Structure

### `ulaw/`
Raw G.711 µ-law source files (`.ulaw`). These are the original unprocessed audio files and are the source of truth for all conversions. Sub-directories mirror the Asterisk sound pack layout (`wx`, `dictate`, `digits`, `silence`, etc.).

### `au/`
G.711 µ-law encoded Sun AU files (`.au`) converted from the `ulaw/` sources. This is the format natively understood by Cisco IOS. Files are generated by `convert_files.sh` and are what gets transferred to the router's flash.

### `wav/`
WAV versions of the same source files, generated alongside the AU files by `convert_files.sh`. Useful for local playback and testing.

### `sit/`
Special Information Tone (SIT) files in AU format. Each file contains a three-tone sequence that the PSTN plays before an announcement to indicate why a call could not be completed (intercept, vacant code, reorder, etc.). See [`sit/README.md`](sit/README.md) for full details and tone specifications.

### `lib/`
Shared Python library used by the scripts and the web app.

| File | Purpose |
|------|---------|
| `lib/au.py` | Reads AU file headers and concatenates two AU files into one, validating that encoding, sample rate, and channel count match. |

### `webapp/`
A Flask web application that provides a browser UI for concatenating a SIT tone with a message file and downloading the result.

- Select a SIT tone from a dropdown
- Search and select a Pat Fleet AU message file
- Download the concatenated AU file ready for TFTP transfer to the router
- Generates a ready-to-use VXML snippet and IOS dial-peer configuration

Run it with:

```bash
cd webapp
python app.py
```

Requires the packages listed in `webapp/requirements.txt`. Install them inside the provided virtualenv:

```bash
cd webapp
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### `ios-configs/`
Example Cisco IOS configuration snippets.

| File | Purpose |
|------|---------|
| `example.vxml` | Minimal VXML 2.0 application that plays a single AU file from flash and disconnects. |
| `ios.cfg` | Example IOS `application` service registration and VoIP dial-peer that invokes the VXML app. |

---

## Scripts

### `convert_files.sh`
Converts all `.ulaw` files in `ulaw/` to AU and/or WAV format using `sox`. Recreates the sub-directory structure under `au/` and `wav/` automatically.

```bash
# Generate both AU and WAV
./convert_files.sh

# Generate AU only
./convert_files.sh au

# Generate WAV only
./convert_files.sh wav
```

Requires `sox` to be installed.

### `concat.py`
Command-line script to concatenate two AU files into one. Wraps the `lib/au.py` library.

```bash
python concat.py sit/1_IC_Intercept.au au/no-longer-in-service.au output.au
```

### `mkzips.sh`
Packages the `au/`, `wav/`, and `ulaw/` directories into zip archives under `zips/` for bulk distribution.

```bash
./mkzips.sh
```

### `clean.sh`
Removes all generated WAV and AU files from `wav/` and `au/`, leaving the `ulaw/` source files untouched.

```bash
./clean.sh
```

---

## Transferring Files to the Router

Once you have a concatenated AU file (from the web app or `concat.py`), host it on a TFTP server and copy it to the router's flash:

```
router# copy tftp flash
Address or name of remote host []? 192.168.1.10
Source filename []? your_file.au
Destination filename [your_file.au]? flash:/audio/your_file.au
```

Verify the transfer:

```
router# dir flash:/audio/
```

---

## Requirements

| Tool | Purpose |
|------|---------|
| `sox` | Converts ulaw source files to AU and WAV formats |
| `ffmpeg` | Generates SIT tone AU files (`sit/generate_sit_files.sh`) |
| Python 3 | Runs `concat.py` and the Flask web app |
| Flask | Web app (installed via `webapp/requirements.txt`) |
